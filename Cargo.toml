[package]
name = "rs"
version = "0.0.39"
edition = "2018"
authors = ["phR0ze"]
license = "MIT OR Apache-2.0"
description = "General purpose reusable Rust code"
readme = "README.md"
homepage = "https://github.com/phR0ze/rs"
repository = "https://github.com/phR0ze/rs"
exclude = [
	"examples",
	"tests",
	"benches/*"
]
include = [
	"src/**/*",
	"Cargo.toml",
	"README.md"
]

[workspace]
members = ["core", "edit", "laconic", "logs", "sys"]

[profile.release]
lto = true

[dependencies]
core = { path = "core" }
laconic = { path = "laconic" }
logs = { path = "logs" }
sys = { path = "sys" }
